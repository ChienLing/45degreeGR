#include <bits/stdc++.h>
#include "Parser.h"
using namespace std;

int main(int argc, char** argv) {
    // BLMR* router = new BLMR;
    // Parser parser(router,argv[1]);
    // parser.read_netlist();
    // parser.print_netlist();
}